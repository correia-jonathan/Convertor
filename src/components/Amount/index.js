import React from 'react';

// Importation des styles du composant
import './styles.scss';

const Amount = () => (
  <section className="amount">
    <p className="amount--value">1.08</p>
    <p className="amount--currency"> Mexican Pesos</p>
  </section>
);

export default Amount;
